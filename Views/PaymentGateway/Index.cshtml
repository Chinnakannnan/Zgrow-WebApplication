@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<script>
    $(document).ready(function () {

        $.getJSON("https://api.ipify.org?format=json", function (data) {
            var ipAddress = data.ip;
            $("#IpAddress").val(ipAddress);
        });
        navigator.geolocation.getCurrentPosition(function (position) {
            var latitude = position.coords.latitude;
            var longitude = position.coords.longitude;
            $("#latitude").val(latitude);
            $("#longitude").val(longitude);
        });
    });
</script>


@using (Html.BeginForm("InitiatePayment", "PaymentGateway", FormMethod.Post, new { @onsubmit = "LoadLoader()" }))@*
@using (Html.BeginForm("InitiatePaymentWithotLogin", "PaymentGateway", FormMethod.Post, new { @onsubmit = "LoadLoader()" }))*@
    { 
<div id="paginationContainer">
    <ul class="pagination"></ul>
        <div class="row justify-content-center" style="margin-top:30px;">
            

        <div class="col-lg-12 col-12">
            <div class="card shadow mb-4">
                <div class="card-body">
                    <!-- Base Style table start -->
                    <div class="col-12">
                            <form  id="PaymentGatewayForm"  class="login">
                        <h5 class="text-primary font-weight-bold">Payment Gateway </h5> <hr/>
                       <div class="row">
                            <div class="col-md-4">
                                <label class="floating-label">
                                    <input type="text" class="form-control" id="Amount" name="Amount"
                                           placeholder="Enter Amount" autocomplete="off" required>
                                    <span class="floating-text">Amount</span>
                                </label>
                            </div>
                            <div class="col-md-4">
                                <label class="floating-label">
                                    <input type="text" class="form-control" id="MobileNumber" name="MobileNumber"
                                           placeholder="Enter Mobile number" autocomplete="off" required>
                                    <span class="floating-text">Mobile number</span>
                                </label>
                            </div>
                            <div class="col-md-4">
                                <label class="floating-label">
                                    <input type="text" class="form-control" id="Name" name="Name"
                                           placeholder="Enter the Description" autocomplete="off" required>
                                    <span class="floating-text">Name</span>
                                </label>
                            </div> 

                        </div>
                        <hr />
                        <div class="row">
                            
                            <div class="col-md-4">
                                <label class="floating-label">
                                    <input type="text" class="form-control" id="MailId" name="MailId"
                                           placeholder="Enter Email Adddress " autocomplete="off" required>
                                    <span class="floating-text">Email Adddress</span>
                                </label>
                            </div>
                            <div class="col-md-4"> 
                                <label class="floating-label">
                                    <input type="text" class="form-control" id="Description" name="Description"
                                           placeholder="Enter the Description" autocomplete="off" required>
                                    <span class="floating-text">Description</span>
                                </label>
                            </div>
                                    <label>
                                        <input type="hidden" class="form-control" id="IpAddress" name="ipAddress">
                                        <input type="hidden" class="form-control" id="latitude" name="latitude">
                                        <input type="hidden" class="form-control" id="longitude" name="longitude">
                                        <input type="checkbox" name="Link" value="Link"> Enable for create a Link
                                    </label>
                            <button type="submit" id="btnsubmit" class="btn btn-primary px-3 ml-md-3" >Submit</button>
                           
                        </div>
                                <hr />
                        </form>
                    </div>
                </div>
            </div>
       
    

        </div>
    </div>

</div>
}
<div class="modal fade" id="alertPopup" tabindex="-1" role="dialog"
     aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="d-flex justify-content-center my-4">
                <i class="fas fa-check-circle text-danger" style="font-size:72px"></i>
            </div>
            <div id="displaydangermessage" class="modal-body text-center"></div>
            <div class="modal-footer align-self-center">
                <button class="btn btn-secondary btn-lg" type="button" data-dismiss="modal">Ok</button>
            </div>
        </div>
    </div>
</div>

<!-- Bootstrap core JavaScript-->
<script src="~/vendor/jquery/jquery.min.js"></script>
<script src="~/vendor/bootstrap/js/bootstrap.bundle.min.js"></script>

<!-- Core plugin JavaScript-->
<script src="~/vendor/jquery-easing/jquery.easing.min.js"></script>

<!-- Custom scripts for all pages-->
<script src="~/js/portal.js"></script>

<!-- Custom scripts for all pages-->
<!-- Custom scripts for all pages-->

<script src="~/js/paymentgateway.js"></script>


